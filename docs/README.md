# VibePull Docs

This folder keeps presentation and report material separate from the runnable project code.

- `VibePull_Project_Report.md`: full written project report.
- `mockups/`: presentation mockups and visual screenshots used while designing the demo.

For the live demo, run the app from the project root with `.\run.ps1` on PowerShell or `run.bat` from Windows cmd.

