{
  "id": "packages_mantine_core_src_components_PinInput_PinInput.story",
  "name": "PinInput.story",
  "source_code": "// packages/@mantine/core/src/components/PinInput/PinInput.story.tsx\nimport { useCallback, useRef, useState } from 'react';\nimport { DEFAULT_THEME, MantineSize } from '../../core';\nimport { Button } from '../Button';\nimport { PinInput } from './PinInput';\n\nexport default { title: 'PinInput' };\n\nexport function Usage() {\n  const [value, setValue] = useState('');\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput id=\"test-id\" value={value} onChange={setValue} />\n      <Button onClick={() => setValue('')}>Reset</Button>\n    </div>\n  );\n}\n\nconst sleep = (ms: number) =>\n  new Promise((resolve) => {\n    setTimeout(resolve, ms);\n  });\n\nexport function AsyncReset() {\n  const [value, setValue] = useState('');\n  const [errorMessage, setErrorMessage] = useState('');\n  const [isLoading, setIsLoading] = useState(false);\n\n  const login = useCallback(async (code: string) => {\n    /* ... */\n    await sleep(500);\n    /* ... */\n\n    throw new Error(`CodeMismatchException: ${code} is not valid`);\n  }, []);\n\n  const handleChange = useCallback(\n    async (newValue: string) => {\n      setValue(newValue);\n\n      if (newValue.length !== 4) {\n        return;\n      }\n\n      setIsLoading(true);\n\n      await login(newValue)\n        .then(() => {\n          setErrorMessage('');\n        })\n        .catch((error) => {\n          setValue('');\n          if (error instanceof Error) {\n            setErrorMessage(error.message);\n          }\n        });\n\n      setIsLoading(false);\n    },\n    [login]\n  );\n\n  return (\n    <>\n      <PinInput disabled={isLoading} onChange={handleChange} value={value} />\n      <div className=\"text-red-500\">{errorMessage}</div>\n      <pre>{JSON.stringify({ value }, null, 2)}</pre>\n    </>\n  );\n}\n\nexport function InputRef() {\n  const inputRef = useRef<HTMLInputElement>(null);\n\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput ref={inputRef} />\n      <Button onClick={() => inputRef.current?.focus()}>Focus first input</Button>\n    </div>\n  );\n}\n\nexport function Unstyled() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput unstyled />\n    </div>\n  );\n}\n\nexport function OnComplete() {\n  const [value, setValue] = useState('');\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput length={5} onComplete={setValue} />\n      Pin: {value}\n    </div>\n  );\n}\n\nexport function ResetControlled() {\n  const [value, setValue] = useState('1234');\n\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput value={value} onChange={setValue} />\n      <Button onClick={() => setValue('')}>Reset</Button>\n    </div>\n  );\n}\n\nexport function ReadOnly() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput readOnly />\n    </div>\n  );\n}\n\nexport function Autofocus() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput autoFocus />\n    </div>\n  );\n}\n\nexport function Controlled() {\n  const [value, setValue] = useState('');\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput value={value} onChange={setValue} />\n      Value: {value}\n    </div>\n  );\n}\n\nexport function Mask() {\n  const [value, setValue] = useState('');\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput value={value} onChange={setValue} mask />\n      Value: {value}\n    </div>\n  );\n}\n\nexport function Sizes() {\n  const sizes = Object.keys(DEFAULT_THEME.fontSizes).map((size) => (\n    <PinInput size={size as MantineSize} key={size} mt=\"md\" />\n  ));\n  return <div style={{ padding: 40 }}>{sizes}</div>;\n}\n\nexport function Number() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput type=\"number\" />\n    </div>\n  );\n}\n\nexport function Tab() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput />\n      <PinInput mt=\"md\" />\n    </div>\n  );\n}\n",
  "description": "",
  "tags": [
    "mantine",
    "registry:ui"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "PinInput_PinInput.story",
      "name": "PinInput.story",
      "path": "packages/@mantine/core/src/components/PinInput/PinInput.story.tsx",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_PinInput_PinInput.story",
      "name": "PinInput.story",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/PinInput/PinInput.story.tsx",
          "content": "import { useCallback, useRef, useState } from 'react';\nimport { DEFAULT_THEME, MantineSize } from '../../core';\nimport { Button } from '../Button';\nimport { PinInput } from './PinInput';\n\nexport default { title: 'PinInput' };\n\nexport function Usage() {\n  const [value, setValue] = useState('');\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput id=\"test-id\" value={value} onChange={setValue} />\n      <Button onClick={() => setValue('')}>Reset</Button>\n    </div>\n  );\n}\n\nconst sleep = (ms: number) =>\n  new Promise((resolve) => {\n    setTimeout(resolve, ms);\n  });\n\nexport function AsyncReset() {\n  const [value, setValue] = useState('');\n  const [errorMessage, setErrorMessage] = useState('');\n  const [isLoading, setIsLoading] = useState(false);\n\n  const login = useCallback(async (code: string) => {\n    /* ... */\n    await sleep(500);\n    /* ... */\n\n    throw new Error(`CodeMismatchException: ${code} is not valid`);\n  }, []);\n\n  const handleChange = useCallback(\n    async (newValue: string) => {\n      setValue(newValue);\n\n      if (newValue.length !== 4) {\n        return;\n      }\n\n      setIsLoading(true);\n\n      await login(newValue)\n        .then(() => {\n          setErrorMessage('');\n        })\n        .catch((error) => {\n          setValue('');\n          if (error instanceof Error) {\n            setErrorMessage(error.message);\n          }\n        });\n\n      setIsLoading(false);\n    },\n    [login]\n  );\n\n  return (\n    <>\n      <PinInput disabled={isLoading} onChange={handleChange} value={value} />\n      <div className=\"text-red-500\">{errorMessage}</div>\n      <pre>{JSON.stringify({ value }, null, 2)}</pre>\n    </>\n  );\n}\n\nexport function InputRef() {\n  const inputRef = useRef<HTMLInputElement>(null);\n\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput ref={inputRef} />\n      <Button onClick={() => inputRef.current?.focus()}>Focus first input</Button>\n    </div>\n  );\n}\n\nexport function Unstyled() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput unstyled />\n    </div>\n  );\n}\n\nexport function OnComplete() {\n  const [value, setValue] = useState('');\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput length={5} onComplete={setValue} />\n      Pin: {value}\n    </div>\n  );\n}\n\nexport function ResetControlled() {\n  const [value, setValue] = useState('1234');\n\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput value={value} onChange={setValue} />\n      <Button onClick={() => setValue('')}>Reset</Button>\n    </div>\n  );\n}\n\nexport function ReadOnly() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput readOnly />\n    </div>\n  );\n}\n\nexport function Autofocus() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput autoFocus />\n    </div>\n  );\n}\n\nexport function Controlled() {\n  const [value, setValue] = useState('');\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput value={value} onChange={setValue} />\n      Value: {value}\n    </div>\n  );\n}\n\nexport function Mask() {\n  const [value, setValue] = useState('');\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput value={value} onChange={setValue} mask />\n      Value: {value}\n    </div>\n  );\n}\n\nexport function Sizes() {\n  const sizes = Object.keys(DEFAULT_THEME.fontSizes).map((size) => (\n    <PinInput size={size as MantineSize} key={size} mt=\"md\" />\n  ));\n  return <div style={{ padding: 40 }}>{sizes}</div>;\n}\n\nexport function Number() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput type=\"number\" />\n    </div>\n  );\n}\n\nexport function Tab() {\n  return (\n    <div style={{ padding: 40 }}>\n      <PinInput />\n      <PinInput mt=\"md\" />\n    </div>\n  );\n}\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  }
}
