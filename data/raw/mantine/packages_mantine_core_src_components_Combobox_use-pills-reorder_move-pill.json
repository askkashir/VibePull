{
  "id": "packages_mantine_core_src_components_Combobox_use-pills-reorder_move-pill",
  "name": "move-pill",
  "source_code": "// packages/@mantine/core/src/components/Combobox/use-pills-reorder/move-pill.ts\nexport type PillReorderPosition = 'before' | 'after';\n\nexport function movePill<T>(\n  value: T[],\n  from: number,\n  to: number,\n  position: PillReorderPosition\n): T[] {\n  if (from === to) {\n    return value;\n  }\n\n  if (position === 'before' && to === from + 1) {\n    return value;\n  }\n\n  if (position === 'after' && to === from - 1) {\n    return value;\n  }\n\n  const result = value.slice();\n  const [item] = result.splice(from, 1);\n\n  let targetIndex = from < to ? to - 1 : to;\n  if (position === 'after') {\n    targetIndex += 1;\n  }\n\n  result.splice(targetIndex, 0, item);\n  return result;\n}\n",
  "description": "",
  "tags": [
    "mantine",
    "registry:ui"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "Combobox_use-pills-reorder_move-pill",
      "name": "move-pill",
      "path": "packages/@mantine/core/src/components/Combobox/use-pills-reorder/move-pill.ts",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_Combobox_use-pills-reorder_move-pill",
      "name": "move-pill",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/Combobox/use-pills-reorder/move-pill.ts",
          "content": "export type PillReorderPosition = 'before' | 'after';\n\nexport function movePill<T>(\n  value: T[],\n  from: number,\n  to: number,\n  position: PillReorderPosition\n): T[] {\n  if (from === to) {\n    return value;\n  }\n\n  if (position === 'before' && to === from + 1) {\n    return value;\n  }\n\n  if (position === 'after' && to === from - 1) {\n    return value;\n  }\n\n  const result = value.slice();\n  const [item] = result.splice(from, 1);\n\n  let targetIndex = from < to ? to - 1 : to;\n  if (position === 'after') {\n    targetIndex += 1;\n  }\n\n  result.splice(targetIndex, 0, item);\n  return result;\n}\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  }
}
