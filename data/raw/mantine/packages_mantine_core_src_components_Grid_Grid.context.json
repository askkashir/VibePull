{
  "id": "packages_mantine_core_src_components_Grid_Grid.context",
  "name": "Grid.context",
  "source_code": "// packages/@mantine/core/src/components/Grid/Grid.context.ts\nimport { createSafeContext, GetStylesApi, MantineSize } from '../../core';\nimport type { GridFactory } from './Grid';\n\nexport type GridBreakpoints = Record<MantineSize, string>;\n\nexport interface GridContextValue {\n  getStyles: GetStylesApi<GridFactory>;\n  grow: boolean | undefined;\n  columns: number;\n  breakpoints: GridBreakpoints | undefined;\n  type: 'container' | 'media' | undefined;\n}\n\nexport const [GridProvider, useGridContext] = createSafeContext<GridContextValue>(\n  'Grid component was not found in tree'\n);\n",
  "description": "",
  "tags": [
    "mantine",
    "registry:ui"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "Grid_Grid.context",
      "name": "Grid.context",
      "path": "packages/@mantine/core/src/components/Grid/Grid.context.ts",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_Grid_Grid.context",
      "name": "Grid.context",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/Grid/Grid.context.ts",
          "content": "import { createSafeContext, GetStylesApi, MantineSize } from '../../core';\nimport type { GridFactory } from './Grid';\n\nexport type GridBreakpoints = Record<MantineSize, string>;\n\nexport interface GridContextValue {\n  getStyles: GetStylesApi<GridFactory>;\n  grow: boolean | undefined;\n  columns: number;\n  breakpoints: GridBreakpoints | undefined;\n  type: 'container' | 'media' | undefined;\n}\n\nexport const [GridProvider, useGridContext] = createSafeContext<GridContextValue>(\n  'Grid component was not found in tree'\n);\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  }
}
