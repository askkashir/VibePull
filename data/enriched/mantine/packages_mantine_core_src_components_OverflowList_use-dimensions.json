{
  "id": "packages_mantine_core_src_components_OverflowList_use-dimensions",
  "name": "use-dimensions",
  "source_code": "// packages/@mantine/core/src/components/OverflowList/use-dimensions.tsx\nimport React, { useEffect, useRef, useState } from 'react';\nimport { flushSync } from 'react-dom';\n\ninterface ResizeObserverDimensions {\n  width: number;\n  height: number;\n  contentWidth: number;\n  contentHeight: number;\n}\nexport function useDimensions<T extends HTMLElement | null>(\n  elementRef: React.RefObject<T>\n): ResizeObserverDimensions | null {\n  const resizeObserverRef = useRef<ResizeObserver | null>(null);\n  const [dimensions, setDimensions] = useState<ResizeObserverDimensions | null>(null);\n\n  useEffect(() => {\n    if (!elementRef.current) {\n      return;\n    }\n\n    resizeObserverRef.current = new ResizeObserver((entries) => {\n      if (entries[0]) {\n        const entry = entries[0];\n        const updateDimensions = () => {\n          setDimensions({\n            width: entry.borderBoxSize[0]?.inlineSize ?? entry.target.clientWidth,\n            height: entry.borderBoxSize[0]?.blockSize ?? entry.target.clientHeight,\n            contentWidth: entry.contentRect.width,\n            contentHeight: entry.contentRect.height,\n          });\n        };\n\n        flushSync(updateDimensions);\n      }\n    });\n\n    resizeObserverRef.current.observe(elementRef.current);\n\n    return () => {\n      if (resizeObserverRef.current) {\n        resizeObserverRef.current.disconnect();\n        resizeObserverRef.current = null;\n      }\n    };\n  }, [elementRef.current]);\n\n  return dimensions;\n}\n",
  "description": "",
  "tags": [
    "dimensions",
    "mantine",
    "other",
    "use"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "OverflowList_use-dimensions",
      "name": "use-dimensions",
      "path": "packages/@mantine/core/src/components/OverflowList/use-dimensions.tsx",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_OverflowList_use-dimensions",
      "name": "use-dimensions",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/OverflowList/use-dimensions.tsx",
          "content": "import React, { useEffect, useRef, useState } from 'react';\nimport { flushSync } from 'react-dom';\n\ninterface ResizeObserverDimensions {\n  width: number;\n  height: number;\n  contentWidth: number;\n  contentHeight: number;\n}\nexport function useDimensions<T extends HTMLElement | null>(\n  elementRef: React.RefObject<T>\n): ResizeObserverDimensions | null {\n  const resizeObserverRef = useRef<ResizeObserver | null>(null);\n  const [dimensions, setDimensions] = useState<ResizeObserverDimensions | null>(null);\n\n  useEffect(() => {\n    if (!elementRef.current) {\n      return;\n    }\n\n    resizeObserverRef.current = new ResizeObserver((entries) => {\n      if (entries[0]) {\n        const entry = entries[0];\n        const updateDimensions = () => {\n          setDimensions({\n            width: entry.borderBoxSize[0]?.inlineSize ?? entry.target.clientWidth,\n            height: entry.borderBoxSize[0]?.blockSize ?? entry.target.clientHeight,\n            contentWidth: entry.contentRect.width,\n            contentHeight: entry.contentRect.height,\n          });\n        };\n\n        flushSync(updateDimensions);\n      }\n    });\n\n    resizeObserverRef.current.observe(elementRef.current);\n\n    return () => {\n      if (resizeObserverRef.current) {\n        resizeObserverRef.current.disconnect();\n        resizeObserverRef.current = null;\n      }\n    };\n  }, [elementRef.current]);\n\n  return dimensions;\n}\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  },
  "visual_summary": "use-dimensions is a use dimensions UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.",
  "style_tags": [],
  "interaction_tags": [],
  "component_type": "other",
  "enrichment_method": "local_fallback",
  "generation_prompt": "Create a production-ready other component with the following characteristics:\n\nName: use-dimensions\nDescription: use-dimensions is a use dimensions UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.\nStyle: \nInteractions: \nTags: dimensions, mantine, other, use\n\nRequirements:\n- Works in React with TypeScript by default but adaptable to Vue, Svelte, or plain HTML/CSS\n- Uses Tailwind CSS for styling\n- Fully accessible with ARIA labels and keyboard navigation\n- Responsive and mobile-friendly\n- No external dependencies beyond React and Tailwind unless necessary\n- Include all necessary imports\n- Export as default with sensible prop types\n\nGenerate clean production-ready code only. No explanations.\nReference implementation (adapt don't copy):\n// packages/@mantine/core/src/components/OverflowList/use-dimensions.tsx\nimport React, { useEffect, useRef, useState } from 'react';\nimport { flushSync } from 'react-dom';\n\ninterface ResizeObserverDimensions {\n  width: number;\n  height: number;\n  contentWidth: number;\n  contentHeight: number;\n}\nexport function useDimensions<T extends HTMLElement | null>(\n  elementRef: React.RefObject<T>\n): ResizeObserverDimensions | null {\n  const resizeObserverRef = useRef<ResizeObserver | null>(null);\n  const ",
  "prompt_version": 2,
  "clean_id": "use-dimensions",
  "display_name": "Use Dimensions",
  "preview_url": "/previews/mantine/packages-mantine-core-src-components-overflowlist-use-dimensions.png"
}
