{
  "id": "packages_mantine_core_src_components_Menu_MenuCheckboxGroup_MenuCheckboxGroup",
  "name": "MenuCheckboxGroup",
  "source_code": "// packages/@mantine/core/src/components/Menu/MenuCheckboxGroup/MenuCheckboxGroup.tsx\nimport { useCallback } from 'react';\nimport { useUncontrolled } from '@mantine/hooks';\nimport { useProps } from '../../../core';\nimport { MenuCheckboxGroupContext } from './MenuCheckboxGroup.context';\n\nexport interface MenuCheckboxGroupProps {\n  /** Controlled selected values */\n  value?: string[];\n\n  /** Uncontrolled default selected values */\n  defaultValue?: string[];\n\n  /** Called with the new array of selected values when a `Menu.CheckboxItem` is toggled */\n  onChange?: (value: string[]) => void;\n\n  /** `Menu.CheckboxItem` components and other elements */\n  children?: React.ReactNode;\n}\n\nexport function MenuCheckboxGroup(_props: MenuCheckboxGroupProps) {\n  const props = useProps('MenuCheckboxGroup', null, _props);\n  const { value, defaultValue, onChange, children } = props;\n\n  const [_value, setValue] = useUncontrolled<string[]>({\n    value,\n    defaultValue,\n    finalValue: [],\n    onChange,\n  });\n\n  const toggle = useCallback(\n    (next: string) => {\n      setValue(_value.includes(next) ? _value.filter((item) => item !== next) : [..._value, next]);\n    },\n    [_value, setValue]\n  );\n\n  return (\n    <MenuCheckboxGroupContext value={{ values: _value, onChange: toggle }}>\n      {children}\n    </MenuCheckboxGroupContext>\n  );\n}\n\nMenuCheckboxGroup.displayName = '@mantine/core/MenuCheckboxGroup';\n",
  "description": "",
  "tags": [
    "checkbox",
    "group",
    "mantine",
    "menu",
    "navbar"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "Menu_MenuCheckboxGroup_MenuCheckboxGroup",
      "name": "MenuCheckboxGroup",
      "path": "packages/@mantine/core/src/components/Menu/MenuCheckboxGroup/MenuCheckboxGroup.tsx",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_Menu_MenuCheckboxGroup_MenuCheckboxGroup",
      "name": "MenuCheckboxGroup",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/Menu/MenuCheckboxGroup/MenuCheckboxGroup.tsx",
          "content": "import { useCallback } from 'react';\nimport { useUncontrolled } from '@mantine/hooks';\nimport { useProps } from '../../../core';\nimport { MenuCheckboxGroupContext } from './MenuCheckboxGroup.context';\n\nexport interface MenuCheckboxGroupProps {\n  /** Controlled selected values */\n  value?: string[];\n\n  /** Uncontrolled default selected values */\n  defaultValue?: string[];\n\n  /** Called with the new array of selected values when a `Menu.CheckboxItem` is toggled */\n  onChange?: (value: string[]) => void;\n\n  /** `Menu.CheckboxItem` components and other elements */\n  children?: React.ReactNode;\n}\n\nexport function MenuCheckboxGroup(_props: MenuCheckboxGroupProps) {\n  const props = useProps('MenuCheckboxGroup', null, _props);\n  const { value, defaultValue, onChange, children } = props;\n\n  const [_value, setValue] = useUncontrolled<string[]>({\n    value,\n    defaultValue,\n    finalValue: [],\n    onChange,\n  });\n\n  const toggle = useCallback(\n    (next: string) => {\n      setValue(_value.includes(next) ? _value.filter((item) => item !== next) : [..._value, next]);\n    },\n    [_value, setValue]\n  );\n\n  return (\n    <MenuCheckboxGroupContext value={{ values: _value, onChange: toggle }}>\n      {children}\n    </MenuCheckboxGroupContext>\n  );\n}\n\nMenuCheckboxGroup.displayName = '@mantine/core/MenuCheckboxGroup';\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  },
  "visual_summary": "MenuCheckboxGroup is a menu checkbox group UI component from the mantine corpus. It provides reusable navbar behavior and styling for React or web UI screens.",
  "style_tags": [],
  "interaction_tags": [
    "interactive"
  ],
  "component_type": "navbar",
  "enrichment_method": "local_fallback",
  "generation_prompt": "Create a production-ready navbar component with the following characteristics:\n\nName: MenuCheckboxGroup\nDescription: MenuCheckboxGroup is a menu checkbox group UI component from the mantine corpus. It provides reusable navbar behavior and styling for React or web UI screens.\nStyle: \nInteractions: interactive\nTags: checkbox, group, mantine, menu, navbar\n\nRequirements:\n- Works in React with TypeScript by default but adaptable to Vue, Svelte, or plain HTML/CSS\n- Uses Tailwind CSS for styling\n- Fully accessible with ARIA labels and keyboard navigation\n- Responsive and mobile-friendly\n- No external dependencies beyond React and Tailwind unless necessary\n- Include all necessary imports\n- Export as default with sensible prop types\n\nGenerate clean production-ready code only. No explanations.\nReference implementation (adapt don't copy):\n// packages/@mantine/core/src/components/Menu/MenuCheckboxGroup/MenuCheckboxGroup.tsx\nimport { useCallback } from 'react';\nimport { useUncontrolled } from '@mantine/hooks';\nimport { useProps } from '../../../core';\nimport { MenuCheckboxGroupContext } from './MenuCheckboxGroup.context';\n\nexport interface MenuCheckboxGroupProps {\n  /** Controlled selected values */\n  value?: string[];\n\n  /** Uncontrolled default selected values */\n  defaultValue?: string[];\n\n  /** Called with the new array of sele",
  "prompt_version": 2,
  "clean_id": "menu-checkbox-group",
  "display_name": "Menu Checkbox Group",
  "preview_url": "/previews/mantine/packages-mantine-core-src-components-menu-menucheckboxgroup-menucheckboxgroup.png"
}
