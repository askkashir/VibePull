{
  "id": "packages_mantine_core_src_components_TagsInput_get-splitted-tags",
  "name": "get-splitted-tags",
  "source_code": "// packages/@mantine/core/src/components/TagsInput/get-splitted-tags.ts\nfunction splitTags(splitChars: string[] | undefined, value: string) {\n  if (!splitChars) {\n    return [value];\n  }\n\n  return value\n    .split(new RegExp(`[${splitChars.join('')}]`))\n    .map((tag) => tag.trim())\n    .filter((tag) => tag !== '');\n}\n\ninterface GetSplittedTagsInput {\n  splitChars: string[] | undefined;\n  allowDuplicates: boolean | undefined;\n  maxTags: number | undefined;\n  value: string;\n  currentTags: string[];\n  isDuplicate?: (value: string, currentValues: string[]) => boolean;\n  onDuplicate?: (value: string) => void;\n}\n\nexport function getSplittedTags({\n  splitChars,\n  allowDuplicates,\n  maxTags,\n  value,\n  currentTags,\n  isDuplicate,\n  onDuplicate,\n}: GetSplittedTagsInput) {\n  const splitted = splitTags(splitChars, value);\n  const merged: string[] = [];\n\n  if (allowDuplicates) {\n    merged.push(...currentTags, ...splitted);\n  } else {\n    merged.push(...currentTags);\n    for (const tag of splitted) {\n      const checkDuplicate = isDuplicate\n        ? (val: string) => isDuplicate(val, merged)\n        : (val: string) => merged.some((t) => t.toLowerCase() === val.toLowerCase());\n\n      if (checkDuplicate(tag)) {\n        onDuplicate?.(tag);\n      } else {\n        merged.push(tag);\n      }\n    }\n  }\n\n  return maxTags ? merged.slice(0, maxTags) : merged;\n}\n",
  "description": "",
  "tags": [
    "get",
    "mantine",
    "other",
    "splitted",
    "tags"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "TagsInput_get-splitted-tags",
      "name": "get-splitted-tags",
      "path": "packages/@mantine/core/src/components/TagsInput/get-splitted-tags.ts",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_TagsInput_get-splitted-tags",
      "name": "get-splitted-tags",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/TagsInput/get-splitted-tags.ts",
          "content": "function splitTags(splitChars: string[] | undefined, value: string) {\n  if (!splitChars) {\n    return [value];\n  }\n\n  return value\n    .split(new RegExp(`[${splitChars.join('')}]`))\n    .map((tag) => tag.trim())\n    .filter((tag) => tag !== '');\n}\n\ninterface GetSplittedTagsInput {\n  splitChars: string[] | undefined;\n  allowDuplicates: boolean | undefined;\n  maxTags: number | undefined;\n  value: string;\n  currentTags: string[];\n  isDuplicate?: (value: string, currentValues: string[]) => boolean;\n  onDuplicate?: (value: string) => void;\n}\n\nexport function getSplittedTags({\n  splitChars,\n  allowDuplicates,\n  maxTags,\n  value,\n  currentTags,\n  isDuplicate,\n  onDuplicate,\n}: GetSplittedTagsInput) {\n  const splitted = splitTags(splitChars, value);\n  const merged: string[] = [];\n\n  if (allowDuplicates) {\n    merged.push(...currentTags, ...splitted);\n  } else {\n    merged.push(...currentTags);\n    for (const tag of splitted) {\n      const checkDuplicate = isDuplicate\n        ? (val: string) => isDuplicate(val, merged)\n        : (val: string) => merged.some((t) => t.toLowerCase() === val.toLowerCase());\n\n      if (checkDuplicate(tag)) {\n        onDuplicate?.(tag);\n      } else {\n        merged.push(tag);\n      }\n    }\n  }\n\n  return maxTags ? merged.slice(0, maxTags) : merged;\n}\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  },
  "visual_summary": "get-splitted-tags is a get splitted tags UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.",
  "style_tags": [],
  "interaction_tags": [],
  "component_type": "other",
  "enrichment_method": "local_fallback",
  "generation_prompt": "Create a production-ready other component with the following characteristics:\n\nName: get-splitted-tags\nDescription: get-splitted-tags is a get splitted tags UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.\nStyle: \nInteractions: \nTags: get, mantine, other, splitted, tags\n\nRequirements:\n- Works in React with TypeScript by default but adaptable to Vue, Svelte, or plain HTML/CSS\n- Uses Tailwind CSS for styling\n- Fully accessible with ARIA labels and keyboard navigation\n- Responsive and mobile-friendly\n- No external dependencies beyond React and Tailwind unless necessary\n- Include all necessary imports\n- Export as default with sensible prop types\n\nGenerate clean production-ready code only. No explanations.\nReference implementation (adapt don't copy):\n// packages/@mantine/core/src/components/TagsInput/get-splitted-tags.ts\nfunction splitTags(splitChars: string[] | undefined, value: string) {\n  if (!splitChars) {\n    return [value];\n  }\n\n  return value\n    .split(new RegExp(`[${splitChars.join('')}]`))\n    .map((tag) => tag.trim())\n    .filter((tag) => tag !== '');\n}\n\ninterface GetSplittedTagsInput {\n  splitChars: string[] | undefined;\n  allowDuplicates: boolean | undefined;\n  maxTags: number | undefined;\n  value: string;\n  currentTags: string[",
  "prompt_version": 2,
  "clean_id": "get-splitted-tags",
  "display_name": "Get Splitted Tags",
  "preview_url": "/previews/mantine/packages-mantine-core-src-components-tagsinput-get-splitted-tags.png"
}
