{
  "id": "packages_mantine_core_src_components_MultiSelect_filter-picked-values",
  "name": "filter-picked-values",
  "source_code": "// packages/@mantine/core/src/components/MultiSelect/filter-picked-values.ts\nimport { Primitive } from '../../core';\nimport { ComboboxParsedItem, isOptionsGroup } from '../Combobox';\n\ninterface FilterPickedTagsInput {\n  data: ComboboxParsedItem<Primitive>[];\n  value: Primitive[];\n}\n\nfunction normalizeValue(value: Primitive) {\n  return typeof value === 'string' ? value.trim().toLowerCase() : value;\n}\n\nexport function filterPickedValues({ data, value }: FilterPickedTagsInput) {\n  const normalizedValue = value.map(normalizeValue);\n\n  const filtered = data.reduce<ComboboxParsedItem<Primitive>[]>((acc, item) => {\n    if (isOptionsGroup(item)) {\n      acc.push({\n        group: item.group,\n        items: item.items.filter(\n          (option) => normalizedValue.indexOf(normalizeValue(option.value)) === -1\n        ),\n      });\n    } else if (normalizedValue.indexOf(normalizeValue(item.value)) === -1) {\n      acc.push(item);\n    }\n\n    return acc;\n  }, []);\n\n  return filtered;\n}\n",
  "description": "",
  "tags": [
    "filter",
    "mantine",
    "other",
    "picked",
    "values"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "MultiSelect_filter-picked-values",
      "name": "filter-picked-values",
      "path": "packages/@mantine/core/src/components/MultiSelect/filter-picked-values.ts",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_MultiSelect_filter-picked-values",
      "name": "filter-picked-values",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/MultiSelect/filter-picked-values.ts",
          "content": "import { Primitive } from '../../core';\nimport { ComboboxParsedItem, isOptionsGroup } from '../Combobox';\n\ninterface FilterPickedTagsInput {\n  data: ComboboxParsedItem<Primitive>[];\n  value: Primitive[];\n}\n\nfunction normalizeValue(value: Primitive) {\n  return typeof value === 'string' ? value.trim().toLowerCase() : value;\n}\n\nexport function filterPickedValues({ data, value }: FilterPickedTagsInput) {\n  const normalizedValue = value.map(normalizeValue);\n\n  const filtered = data.reduce<ComboboxParsedItem<Primitive>[]>((acc, item) => {\n    if (isOptionsGroup(item)) {\n      acc.push({\n        group: item.group,\n        items: item.items.filter(\n          (option) => normalizedValue.indexOf(normalizeValue(option.value)) === -1\n        ),\n      });\n    } else if (normalizedValue.indexOf(normalizeValue(item.value)) === -1) {\n      acc.push(item);\n    }\n\n    return acc;\n  }, []);\n\n  return filtered;\n}\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  },
  "visual_summary": "filter-picked-values is a filter picked values UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.",
  "style_tags": [],
  "interaction_tags": [],
  "component_type": "other",
  "enrichment_method": "local_fallback",
  "generation_prompt": "Create a production-ready other component with the following characteristics:\n\nName: filter-picked-values\nDescription: filter-picked-values is a filter picked values UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.\nStyle: \nInteractions: \nTags: filter, mantine, other, picked, values\n\nRequirements:\n- Works in React with TypeScript by default but adaptable to Vue, Svelte, or plain HTML/CSS\n- Uses Tailwind CSS for styling\n- Fully accessible with ARIA labels and keyboard navigation\n- Responsive and mobile-friendly\n- No external dependencies beyond React and Tailwind unless necessary\n- Include all necessary imports\n- Export as default with sensible prop types\n\nGenerate clean production-ready code only. No explanations.\nReference implementation (adapt don't copy):\n// packages/@mantine/core/src/components/MultiSelect/filter-picked-values.ts\nimport { Primitive } from '../../core';\nimport { ComboboxParsedItem, isOptionsGroup } from '../Combobox';\n\ninterface FilterPickedTagsInput {\n  data: ComboboxParsedItem<Primitive>[];\n  value: Primitive[];\n}\n\nfunction normalizeValue(value: Primitive) {\n  return typeof value === 'string' ? value.trim().toLowerCase() : value;\n}\n\nexport function filterPickedValues({ data, value }: FilterPickedTagsInput) {\n  const normalizedV",
  "prompt_version": 2,
  "clean_id": "filter-picked-values",
  "display_name": "Filter Picked Values",
  "preview_url": "/previews/mantine/packages-mantine-core-src-components-multiselect-filter-picked-values.png"
}
