{
  "id": "packages_mantine_core_src_components_RingProgress_Curve_get-curve-props",
  "name": "get-curve-props",
  "source_code": "// packages/@mantine/core/src/components/RingProgress/Curve/get-curve-props.ts\ninterface GetCurveProps {\n  size: number;\n  thickness: number;\n  sum: number;\n  value: number | undefined;\n  root: boolean | undefined;\n  offset: number;\n}\n\nexport function getCurveProps({ size, thickness, sum, value, root, offset }: GetCurveProps) {\n  const radius = (size * 0.9 - thickness * 2) / 2;\n  const deg = (Math.PI * radius * 2) / 100;\n\n  const strokeDasharray =\n    root || value === undefined\n      ? `${(100 - sum) * deg}, ${sum * deg}`\n      : `${value * deg}, ${(100 - value) * deg}`;\n\n  return {\n    strokeWidth: Number.isNaN(thickness) ? 12 : thickness,\n    cx: size / 2 || 0,\n    cy: size / 2 || 0,\n    r: radius || 0,\n    transform: root ? `scale(1, -1) translate(0, -${size})` : undefined,\n    strokeDasharray,\n    strokeDashoffset: root ? 0 : offset || 0,\n  };\n}\n",
  "description": "",
  "tags": [
    "curve",
    "get",
    "mantine",
    "other",
    "props"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "RingProgress_Curve_get-curve-props",
      "name": "get-curve-props",
      "path": "packages/@mantine/core/src/components/RingProgress/Curve/get-curve-props.ts",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_RingProgress_Curve_get-curve-props",
      "name": "get-curve-props",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/RingProgress/Curve/get-curve-props.ts",
          "content": "interface GetCurveProps {\n  size: number;\n  thickness: number;\n  sum: number;\n  value: number | undefined;\n  root: boolean | undefined;\n  offset: number;\n}\n\nexport function getCurveProps({ size, thickness, sum, value, root, offset }: GetCurveProps) {\n  const radius = (size * 0.9 - thickness * 2) / 2;\n  const deg = (Math.PI * radius * 2) / 100;\n\n  const strokeDasharray =\n    root || value === undefined\n      ? `${(100 - sum) * deg}, ${sum * deg}`\n      : `${value * deg}, ${(100 - value) * deg}`;\n\n  return {\n    strokeWidth: Number.isNaN(thickness) ? 12 : thickness,\n    cx: size / 2 || 0,\n    cy: size / 2 || 0,\n    r: radius || 0,\n    transform: root ? `scale(1, -1) translate(0, -${size})` : undefined,\n    strokeDasharray,\n    strokeDashoffset: root ? 0 : offset || 0,\n  };\n}\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  },
  "visual_summary": "get-curve-props is a get curve props UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.",
  "style_tags": [],
  "interaction_tags": [],
  "component_type": "other",
  "enrichment_method": "local_fallback",
  "generation_prompt": "Create a production-ready other component with the following characteristics:\n\nName: get-curve-props\nDescription: get-curve-props is a get curve props UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.\nStyle: \nInteractions: \nTags: curve, get, mantine, other, props\n\nRequirements:\n- Works in React with TypeScript by default but adaptable to Vue, Svelte, or plain HTML/CSS\n- Uses Tailwind CSS for styling\n- Fully accessible with ARIA labels and keyboard navigation\n- Responsive and mobile-friendly\n- No external dependencies beyond React and Tailwind unless necessary\n- Include all necessary imports\n- Export as default with sensible prop types\n\nGenerate clean production-ready code only. No explanations.\nReference implementation (adapt don't copy):\n// packages/@mantine/core/src/components/RingProgress/Curve/get-curve-props.ts\ninterface GetCurveProps {\n  size: number;\n  thickness: number;\n  sum: number;\n  value: number | undefined;\n  root: boolean | undefined;\n  offset: number;\n}\n\nexport function getCurveProps({ size, thickness, sum, value, root, offset }: GetCurveProps) {\n  const radius = (size * 0.9 - thickness * 2) / 2;\n  const deg = (Math.PI * radius * 2) / 100;\n\n  const strokeDasharray =\n    root || value === undefined\n      ? `${(100 ",
  "prompt_version": 2,
  "clean_id": "get-curve-props",
  "display_name": "Get Curve Props",
  "preview_url": "/previews/mantine/packages-mantine-core-src-components-ringprogress-curve-get-curve-props.png"
}
