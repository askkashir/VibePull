{
  "id": "packages_mantine_core_src_components_TreeSelect_get-checked-values-by-strategy",
  "name": "get-checked-values-by-strategy",
  "source_code": "// packages/@mantine/core/src/components/TreeSelect/get-checked-values-by-strategy.ts\nimport type { TreeNodeData } from '../Tree';\nimport { getAllCheckedNodes } from '../Tree/get-all-checked-nodes/get-all-checked-nodes';\nimport { getChildrenNodesValues } from '../Tree/get-children-nodes-values/get-children-nodes-values';\nimport { isNodeChecked } from '../Tree/is-node-checked/is-node-checked';\n\nexport type CheckedStrategy = 'all' | 'parent' | 'child';\n\nexport function expandToLeafChecked(value: string[], data: TreeNodeData[]): string[] {\n  const leaves = new Set<string>();\n  for (const v of value) {\n    for (const leaf of getChildrenNodesValues(v, data)) {\n      leaves.add(leaf);\n    }\n  }\n  return Array.from(leaves);\n}\n\nfunction getTopmostCheckedParents(data: TreeNodeData[], checkedState: string[]): string[] {\n  const result: string[] = [];\n  for (const node of data) {\n    if (isNodeChecked(node.value, data, checkedState)) {\n      result.push(node.value);\n    } else if (Array.isArray(node.children) && node.children.length > 0) {\n      result.push(...getTopmostCheckedParents(node.children, checkedState));\n    }\n  }\n  return result;\n}\n\nexport function checkedToValue(\n  checkedState: string[],\n  data: TreeNodeData[],\n  strategy: CheckedStrategy\n): string[] {\n  if (checkedState.length === 0) {\n    return [];\n  }\n\n  switch (strategy) {\n    case 'child':\n      return checkedState;\n    case 'all':\n      return getAllCheckedNodes(data, checkedState)\n        .result.filter((n) => n.checked)\n        .map((n) => n.value);\n    case 'parent':\n      return getTopmostCheckedParents(data, checkedState);\n    default:\n      return checkedState;\n  }\n}\n",
  "description": "",
  "tags": [
    "by",
    "checked",
    "get",
    "mantine",
    "other",
    "strategy",
    "values"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "TreeSelect_get-checked-values-by-strategy",
      "name": "get-checked-values-by-strategy",
      "path": "packages/@mantine/core/src/components/TreeSelect/get-checked-values-by-strategy.ts",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_TreeSelect_get-checked-values-by-strategy",
      "name": "get-checked-values-by-strategy",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/TreeSelect/get-checked-values-by-strategy.ts",
          "content": "import type { TreeNodeData } from '../Tree';\nimport { getAllCheckedNodes } from '../Tree/get-all-checked-nodes/get-all-checked-nodes';\nimport { getChildrenNodesValues } from '../Tree/get-children-nodes-values/get-children-nodes-values';\nimport { isNodeChecked } from '../Tree/is-node-checked/is-node-checked';\n\nexport type CheckedStrategy = 'all' | 'parent' | 'child';\n\nexport function expandToLeafChecked(value: string[], data: TreeNodeData[]): string[] {\n  const leaves = new Set<string>();\n  for (const v of value) {\n    for (const leaf of getChildrenNodesValues(v, data)) {\n      leaves.add(leaf);\n    }\n  }\n  return Array.from(leaves);\n}\n\nfunction getTopmostCheckedParents(data: TreeNodeData[], checkedState: string[]): string[] {\n  const result: string[] = [];\n  for (const node of data) {\n    if (isNodeChecked(node.value, data, checkedState)) {\n      result.push(node.value);\n    } else if (Array.isArray(node.children) && node.children.length > 0) {\n      result.push(...getTopmostCheckedParents(node.children, checkedState));\n    }\n  }\n  return result;\n}\n\nexport function checkedToValue(\n  checkedState: string[],\n  data: TreeNodeData[],\n  strategy: CheckedStrategy\n): string[] {\n  if (checkedState.length === 0) {\n    return [];\n  }\n\n  switch (strategy) {\n    case 'child':\n      return checkedState;\n    case 'all':\n      return getAllCheckedNodes(data, checkedState)\n        .result.filter((n) => n.checked)\n        .map((n) => n.value);\n    case 'parent':\n      return getTopmostCheckedParents(data, checkedState);\n    default:\n      return checkedState;\n  }\n}\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  },
  "visual_summary": "get-checked-values-by-strategy is a get checked values by UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.",
  "style_tags": [],
  "interaction_tags": [],
  "component_type": "other",
  "enrichment_method": "local_fallback",
  "generation_prompt": "Create a production-ready other component with the following characteristics:\n\nName: get-checked-values-by-strategy\nDescription: get-checked-values-by-strategy is a get checked values by UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.\nStyle: \nInteractions: \nTags: by, checked, get, mantine, other, strategy, values\n\nRequirements:\n- Works in React with TypeScript by default but adaptable to Vue, Svelte, or plain HTML/CSS\n- Uses Tailwind CSS for styling\n- Fully accessible with ARIA labels and keyboard navigation\n- Responsive and mobile-friendly\n- No external dependencies beyond React and Tailwind unless necessary\n- Include all necessary imports\n- Export as default with sensible prop types\n\nGenerate clean production-ready code only. No explanations.\nReference implementation (adapt don't copy):\n// packages/@mantine/core/src/components/TreeSelect/get-checked-values-by-strategy.ts\nimport type { TreeNodeData } from '../Tree';\nimport { getAllCheckedNodes } from '../Tree/get-all-checked-nodes/get-all-checked-nodes';\nimport { getChildrenNodesValues } from '../Tree/get-children-nodes-values/get-children-nodes-values';\nimport { isNodeChecked } from '../Tree/is-node-checked/is-node-checked';\n\nexport type CheckedStrategy = 'all' | 'parent' | 'child';\n\nexport function expandToLeafChecked(value: s",
  "prompt_version": 2,
  "clean_id": "get-checked-values-by-strategy",
  "display_name": "Get Checked Values By Strategy",
  "preview_url": "/previews/mantine/packages-mantine-core-src-components-treeselect-get-checked-values-by-strategy.png"
}
