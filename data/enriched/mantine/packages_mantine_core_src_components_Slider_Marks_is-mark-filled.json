{
  "id": "packages_mantine_core_src_components_Slider_Marks_is-mark-filled",
  "name": "is-mark-filled",
  "source_code": "// packages/@mantine/core/src/components/Slider/Marks/is-mark-filled.ts\ninterface IsMarkFilled {\n  mark: { value: number; label?: any };\n  offset?: number;\n  value: number;\n  inverted?: boolean;\n  startPointValue?: number;\n}\n\nexport function isMarkFilled({\n  mark,\n  offset,\n  value,\n  inverted = false,\n  startPointValue,\n}: IsMarkFilled) {\n  if (typeof startPointValue === 'number' && !inverted) {\n    return (\n      (mark.value >= startPointValue && mark.value <= value) ||\n      (mark.value <= startPointValue && mark.value >= value)\n    );\n  }\n\n  return inverted\n    ? typeof offset === 'number'\n      ? mark.value <= offset || mark.value >= value\n      : mark.value >= value\n    : typeof offset === 'number'\n      ? mark.value >= offset && mark.value <= value\n      : mark.value <= value;\n}\n",
  "description": "",
  "tags": [
    "filled",
    "is",
    "mantine",
    "mark",
    "other"
  ],
  "server": "mantine",
  "metadata": {
    "listed_component": {
      "id": "Slider_Marks_is-mark-filled",
      "name": "is-mark-filled",
      "path": "packages/@mantine/core/src/components/Slider/Marks/is-mark-filled.ts",
      "source": "zip_archive"
    },
    "registry_item": {
      "id": "packages_mantine_core_src_components_Slider_Marks_is-mark-filled",
      "name": "is-mark-filled",
      "type": "registry:ui",
      "files": [
        {
          "path": "packages/@mantine/core/src/components/Slider/Marks/is-mark-filled.ts",
          "content": "interface IsMarkFilled {\n  mark: { value: number; label?: any };\n  offset?: number;\n  value: number;\n  inverted?: boolean;\n  startPointValue?: number;\n}\n\nexport function isMarkFilled({\n  mark,\n  offset,\n  value,\n  inverted = false,\n  startPointValue,\n}: IsMarkFilled) {\n  if (typeof startPointValue === 'number' && !inverted) {\n    return (\n      (mark.value >= startPointValue && mark.value <= value) ||\n      (mark.value <= startPointValue && mark.value >= value)\n    );\n  }\n\n  return inverted\n    ? typeof offset === 'number'\n      ? mark.value <= offset || mark.value >= value\n      : mark.value >= value\n    : typeof offset === 'number'\n      ? mark.value >= offset && mark.value <= value\n      : mark.value <= value;\n}\n"
        }
      ]
    },
    "ingest_method": "zip_fallback"
  },
  "visual_summary": "is-mark-filled is a is mark filled UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.",
  "style_tags": [],
  "interaction_tags": [],
  "component_type": "other",
  "enrichment_method": "local_fallback",
  "generation_prompt": "Create a production-ready other component with the following characteristics:\n\nName: is-mark-filled\nDescription: is-mark-filled is a is mark filled UI component from the mantine corpus. It provides reusable other behavior and styling for React or web UI screens.\nStyle: \nInteractions: \nTags: filled, is, mantine, mark, other\n\nRequirements:\n- Works in React with TypeScript by default but adaptable to Vue, Svelte, or plain HTML/CSS\n- Uses Tailwind CSS for styling\n- Fully accessible with ARIA labels and keyboard navigation\n- Responsive and mobile-friendly\n- No external dependencies beyond React and Tailwind unless necessary\n- Include all necessary imports\n- Export as default with sensible prop types\n\nGenerate clean production-ready code only. No explanations.\nReference implementation (adapt don't copy):\n// packages/@mantine/core/src/components/Slider/Marks/is-mark-filled.ts\ninterface IsMarkFilled {\n  mark: { value: number; label?: any };\n  offset?: number;\n  value: number;\n  inverted?: boolean;\n  startPointValue?: number;\n}\n\nexport function isMarkFilled({\n  mark,\n  offset,\n  value,\n  inverted = false,\n  startPointValue,\n}: IsMarkFilled) {\n  if (typeof startPointValue === 'number' && !inverted) {\n    return (\n      (mark.value >= startPointValue && mark.value <= value) ||\n      (mark.value <= st",
  "prompt_version": 2,
  "clean_id": "is-mark-filled",
  "display_name": "Is Mark Filled",
  "preview_url": "/previews/mantine/packages-mantine-core-src-components-slider-marks-is-mark-filled.png"
}
